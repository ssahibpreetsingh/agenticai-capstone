# Secrets — never commit
.env

# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Vector DB (regenerated by running the scripts — keep repo light)
outputs/chroma_db/

# OS
.DS_Store
Thumbs.db

# Keep these tracked (override any accidental broad ignore rules above):
!outputs/chunks.json
!outputs/generation_samples.json
!outputs/evaluation_results.json
!outputs/screenshots/
!outputs/RESULTS_TEMPLATE.md
